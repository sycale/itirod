import React, { useState } from "react";

export default function CreateTest() {
  const [questions, setQuestions] = useState([]);
  const [criterias, setCriterias] = useState([]);

  const renderQuestionsInput = () => {
      return (
            // {questions.}
      )
  };
  const renderCriteriasInput = () => {};
  return <div>C</div>;
}
